AUX_SOURCE_DIRECTORY(. SRC )
FILE(GLOB_RECURSE INC "*.h")


INCLUDE_DIRECTORIES(
${COMMON_INCLUDE_DIR};
)

LINK_DIRECTORIES(
${COMMON_LIBRARY_DIR};
)


ADD_EXE( "AOIResearch" "${INC}" "${SRC}" )

#排除掉不需要的文件，设置预编译
SET(STDAFX_LIST ${SRC})
#LIST(REMOVE_ITEM STDAFX_LIST some.cpp)
ADD_PRECOMPILED_HEADER(SimpleServer "pch.h" "./pch.cpp" STDAFX_LIST)


SET_PROPERTY(TARGET "AOIResearch" PROPERTY FOLDER "Server")

